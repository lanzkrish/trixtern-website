import React from "react";

const ReviewCard: React.FC = () => {
  return (
    <figure>
      <p className="relative z-10 bg-red-600 text-base leading-6 text-gray-50 font-semibold uppercase whitespace-nowrap animate-marquee">
        Currently we are in stealth mode.
      </p>
    </figure>
  );
};

export default ReviewCard;
